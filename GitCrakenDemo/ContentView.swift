//
//  ContentView.swift
//  GitCrakenDemo
//
//  Created by Pavlo Moroz on 2024-06-04.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, private company")
            Button {
                print("")
            } label: {
                Text("Test22222")
            }
            
            Button {
                print("")
            } label: {
                Text("Test2222")
            }

        }
        .padding()
        .padding()
        .padding()
        .padding()
        .padding()
        .padding()
        .padding()
        .padding()
        .padding()
    }
}

#Preview {
    ContentView()
}
